<script>

$.ajax({
    url: "https://data.cityofnewyork.us/resource/muk7-ct23.json?$where=(starts_with(house_number, '2836') AND starts_with(house_street, 'ZULETTE AVENUE'))",
    type: "GET",
    data: {
      "$limit" : 10000000,
      "$$app_token" : "XXXXXXXXXXXXXXX"
    }
}).done(function(data) {
  alert("Retrieved " + data.length + " records from the dataset!");
  console.log(data);
});

</script>